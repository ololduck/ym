#[cfg(test)]
mod tests {
    #[test]
    fn merge_different_documents() {
        assert!(false);
    }

    #[test]
    fn merge_same_keys() {
        assert!(false);
    }

    #[test]
    fn merge_wildcard() {
        assert!(false);
    }
}
